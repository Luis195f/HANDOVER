# Minimal project-scoped Codex configuration for HANDOVER.
# Keep this file conservative and limited to locally verified keys.

model = "gpt-5.4"
model_reasoning_effort = "high"
approval_policy = "on-request"
sandbox_mode = "workspace-write"
