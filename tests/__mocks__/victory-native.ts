import React from "react";

type AnyProps = Record<string, any>;

const Null = (_props: AnyProps) => null;

export const VictoryLine = Null;
export const VictoryChart = Null;
export const VictoryAxis = Null;
export const VictoryTheme = {};
export const VictoryLegend = Null;
export const VictoryScatter = Null;

export default {
  VictoryLine,
  VictoryChart,
  VictoryAxis,
  VictoryTheme,
  VictoryLegend,
  VictoryScatter,
};
